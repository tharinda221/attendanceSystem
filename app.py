from config import *
import socket

if __name__ == '__main__':
    # s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    # s.connect(('8.8.8.8', 0))
    # ipAddress = s.getsockname()[0]
    app.run()
